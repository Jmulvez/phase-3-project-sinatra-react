import React from 'react';

function Home({ movies }) {
    const getMovies = movies.map((movie) => {
        return <div>
                    
                </div>
    })
    return (
        <div>
            Home Page
        </div>
    );
}

export default Home;